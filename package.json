{
  "name": "bodymaps-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-native CT and segmentation viewer. No plugins, no server, no medical imaging libraries.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
